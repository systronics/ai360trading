# CLAUDE.md — AI360Trading Session Guide
> Auto-read by Claude Code every session. Follow every rule. No exceptions.

---

## OWNER
**Amit Kumar, Haridwar, Uttarakhand, India**
**6 family members depend on this system for their livelihood.**
Family is non-technical — cannot fix a broken system.
One broken file = zero income that day. Treat every edit as critical.

---

## PROJECT
**AI360Trading** — Fully automated trading signals + content income platform.
- ₹0/month cost forever (free tiers only)
- Paper trading only — Phase 1–3. Do NOT connect Dhan API or execute real trades.
- Everything runs on GitHub Actions — zero manual daily work required.

---

## FIRST THING EVERY SESSION

```
1. Run smartsync.bat   (syncs PC ↔ GitHub — ensures you have latest files)
2. Read CHANGELOG.md   (what changed last)
3. Read SESSION.md     (pending tasks, known issues, current versions)
4. Read the actual file before touching it — NEVER use memory or stale reads
```

---

## CURRENT FILE VERSIONS
*(Read from actual files — verified 2026-07-28: every row below cross-checked against each file's real docstring/VERSION constant, 1:1, not just spot-checked. Update this date whenever a full pass like that is repeated — don't let it go stale the way it did sitting at "2026-07-14" for 2 weeks while the table itself kept changing underneath it.)*

### Trading Engine
| File | Version |
|---|---|
| `trading_bot.py` | v15.34 (2026-07-30 same-session follow-up: owner asked to also fix the cross-contamination gap flagged-but-not-fixed in the v15.33 pass — the cooldown key was purely symbol-based, so a losing OPTIONS trade locked out a stock's CASH/SWING re-entry and vice versa. Fixed: new `_cooldown_family(ttype)` → OPTION/CASH/STOCK; `family` param threaded through `get_reentry_cooldown_date()`/`set_reentry_cooldown()`/`get_loss_cooldown_date()`/`set_loss_cooldown()`/`check_reentry_allowed()`; BotMemory key now carries family (`NSE_TCS_OPTION_LRECD_...` vs `NSE_TCS_STOCK_LRECD_...`); `check_all_entry_filters()` gained `ttype=""` to compute family at check time. Side effect: CASH already bypassed this check on the way in, so isolating it also stops a CASH loss from silently cooldowning a future OPTIONS/SWING signal — closes that asymmetry too. Verified with 8 boundary tests (fresh, same-family-blocked, cross-family-allowed both directions incl. CASH, next-day recovery, target-hit family-scoping, real ttype-string mapping against "CASH Intraday"/"📊 Options Alert"/"🎯 SWING (Breakout)") — all 8 passed; py_compile clean. Also caught and fixed a self-introduced version-labeling slip from the v15.33 pass (several docstrings were mislabeled "v15.34" when they should've said v15.33 — fixed via an ordered two-pass rename, re-verified with tests afterward). v15.33 (2026-07-30 owner audit: owner theory — a stock that breaks out, gets stopped on a same-day retest, then genuinely resumes the breakout the next trading day was being locked out of that real move. Confirmed: `_exit_trade()`'s `is_loss = exit_p < ent; set_cd = is_target_hit or is_loss` set the SAME `REENTRY_COOLDOWN_DAYS=5` (5 trading days, calibrated in v15.0 for a different problem — IDEA re-bought the SAME DAY it hit target, chasing an extended move) for EVERY losing exit too, including a fractional-percent same-day shakeout. v15.17 (2026-07-12) had reused that 5-day number for losses to stop EICHERMOT being re-bought at the same ceiling twice, but never gave losses their own calibrated duration. Owner picked 1 trading day (of 4 presented options) for the loss case; target-hit cooldown stays 5 days, unchanged. Fixed: new `LOSS_REENTRY_COOLDOWN_DAYS=1` constant, new `set_loss_cooldown()`/`get_loss_cooldown_date()` (BotMemory prefix `_LRECD_`, parallel to the existing `_RECD_` target-hit pair), `check_reentry_allowed()` checks both and blocks if either active, `_exit_trade()` routes to the correct one, exit Telegram + Good Morning digest now report the right duration per cooldown type (was hardcoded to always say 5). The resistance-room veto (v15.17, `MIN_TARGET_ROOM_ATR`) — the precise defence against re-buying the SAME ceiling — is untouched. Verified with 5 hand-built boundary tests via direct function import (fresh/no-cooldown allowed, same-day-after-loss blocked, next-day-after-loss now allowed, next-day-after-target-hit still blocked, 6+ days-after-target-hit allowed) — all 5 passed; py_compile clean. Not fixed this pass (lower priority, not asked): cooldown key is symbol-only, not trade-type-specific — a losing OPTIONS trade still locks a stock's CASH/SWING re-entry for the same duration and vice versa; CASH entries already bypass the cooldown check entirely (never call `check_all_entry_filters`). Same session: full sheets inventory (all 12 tabs) + RS-boundary cross-check (`trading_bot.py` RS_MIN_PCT=5.0 vs `appscript.gs` LATE_ENTRY_MIN_RS=5) both came back clean, no bug — RS boundary is correctly locked and consistent between engines. v15.32 (2026-07-28 owner-reported bug: subscriber screenshots showed the SAME "TSL UPDATE" alert (identical LTP/SL/P&L) firing every ~5 min for nearly an hour on one live trade (ETERNAL). Root cause: `calc_new_tsl()`'s breakeven/lock1/chandelier formulas are deterministic functions of unchanging inputs (e.g. `ent*1.002`) and return an unrounded float, while the stored TSL is rounded to paisa (`int(round(price*100))`) — so a re-computation of the SAME real value (e.g. 296.592 vs stored 296.59) sat a fraction of a paisa above the stored value on every tick, and the plain `new_tsl > cur_tsl` float compare was true every single time even though nothing had actually changed. Reproduced exactly with the real screenshot numbers (ent=296.00 → recomputes to 296.592 forever, stored rounds to 296.59, old guard fires every cycle). Fixed: the comparison guard now compares in integer paise (`int(round(new_tsl*100)) > int(round(cur_tsl*100))`), matching the storage precision — two computations of the same real value now compare equal, not greater, while a genuine improvement (price moved, even by 1 paisa) still correctly fires. Verified both cases with the real numbers before shipping. No change to SL/exit correctness — the stored TSL value itself was never wrong, only the alert was spamming. 2026-07-26 Telegram text audit: `send_midday_pulse()` hardcoded "Entry window ends 2:30 PM" for the bullish case, but `ENTRY_WINDOW_BULLISH_END` moved 14:30→15:05 back in v15.29 (2026-07-23) — subscribers were being told the window closed 35 minutes early, every day since. Fixed to read the real constant, same pattern `send_good_morning()` already used correctly. `appscript.gs` also got a stale-comment-only fix (said "10:30 AM", code already read the live `CONFIG.CASH_ENTRY_WINDOW` constant — no behavior change, not redeployed via clasp). v15.30 (2026-07-26 full-system audit: `CASH_ENTRY_WINDOW_END` was still `(10, 30)` while appscript.gs had already extended its own `CONFIG.CASH_ENTRY_WINDOW` to 13:30 (v15.29/30, to catch 4%+ gap/catalyst movers building their move after 10:30) — the promotion-side cutoff was never updated to match, so afternoon cash WAITING rows were queued by appscript.gs and then silently starved here, never promoted to TRADED. Fixed to `(13, 30)` to match. Also corrected a stray "v15.30" comment label on the ENTRY_WINDOW_BULLISH_END line to "v15.29" (that change already shipped under v15.29, confirmed against this table's own history — the mislabel would have collided with this session's real v15.30). No gate/threshold logic touched otherwise. 2026-07-23 post-midnight: `ENTRY_WINDOW_BULLISH_END` moved 14:30→15:05 — a live 07-21 candidate (M&MFIN) appeared ~15:07 and was time-blocked every tick to close; TIME was the #2 blocker (29% of all 58-session filter checks) after RSI. Bearish window (11:00) deliberately NOT touched — tied to the v15.6 "4 losses" evidence. 5/5 boundary-case unit test passed (incl. new 15:05/15:06 edge). 2026-07-22 post-midnight: `VERSION` constant was stuck at "v15.28" — the RSI dip-tolerance fix below was committed/documented as v15.29 everywhere except this one banner-text string; fixed, cosmetic only, the actual fix was confirmed live/running the whole time via log inspection. 2026-07-21 late night: RSI hot-leader exception given a small dip-tolerance noise floor — `RSI_HOT_DIP_TOLERANCE=0.3`; the "stock up today" condition now accepts `day_pct > -0.3%` instead of requiring `> 0` exactly. Evidence: 58-session/3.5-week log audit found RSI was the #1 entry blocker (45% of all filter-checks, vs RS 14% and Volume 12%), and 812 of those 1,311 RSI blocks (62%) were sitting in the 65-75 exception window with NIFTY regime bullish ~90% of the time — day_pct was flickering across zero on live 5-min ticks (a real leader doesn't move in a straight line), not the regime or a genuinely red day, killing the exception on noise rather than substance. RSI_MAX_BULLISH(65)/RSI_HOT_MAX(75) themselves were NOT touched — only the noisy zero-crossing check. 13/13 unit tests pass (boundary cases at exactly -0.3%, fail-closed on missing data, fail-open on missing RSI, base cap unchanged above 75 and in bearish regime); py_compile clean. v15.28: volume-gate shadow tracker — new ShadowLedger sheet tab; candidates that pass every filter except the 1.5x volume gate get logged as never-traded phantom entries and scored later with the same target/SL/TIME_STOP_DAYS rules real trades use, to measure whether the hard gate is well-calibrated; `BOT_MODE=shadow_report` for an on-demand summary; observation-only, never touches AlertLog/History/BotMemory trade-state; v15.27: full-system audit — removed dead ACCUM_ memory key that clean_mem never pruned; _read_atr_from_nifty200/_read_opt_tag_from_nifty200 switched from hardcoded row[28]/row[35] to header-name lookup (_find_nifty200_col) so a future inserted column can't silently corrupt SL/target/option math; stale "12-day"/BUY_PE comments fixed; v15.26 OWNER RULE — options are INTRADAY/1-2 day holds only; OPTION_MAX_TDAYS=2 replaces the base 12-trading-day rule for ALL option ledger rows (20% cap + expiry-week backstop unchanged); entry alerts carry _trade_style_lines — CASH/MTF vs cash vs accumulate + "Entry valid jab tak price SL ke upar" + TradingView chart line (DAILY/15m/WEEKLY); v15.25 option ledger follows the alert's rules; v15.24 unified strike grid + premium-aware option P/L; v15.23 full bug pack) |
| `entry_quality.py` | v1.0 (reversal veto + target room + composite ranking) |
| `option_intelligence.py` | v1.4 (2026-07-20: doc-only fix — stale 'BUY_PE' removed from action docstring, PE path confirmed dead since buy-side-only 2026-06-05; 2026-07-18: option alert wording = intraday/max 1-2 day hold, current-expiry rationale, 2-day exit; v1.3 strike table unified w/ appscript + est_option_leverage; v1.2 earnings via bm_data rows) |
| `institutional_edges.py` | v1.1 (2026-07-16: volume gate time-adjusted — partial-day reading ÷ expected session fraction, 1.5× bar unchanged) |
| `fetch_earnings.py` | v1.2 (2026-07-17: BSE long names → real NSE symbols via official EQUITY_L.csv (first-token keys were mostly dead); unmappable dropped; fail-open to old behavior; v1.1 atomic BotMemory write) |
| `fetch_bhavcopy.py` | v1.0 (Batch 4, Mon-Fri 20:00 IST) |
| `fetch_smallmidcap.py` | v1.5 (2026-07-18: easy-Hinglish digest — CASH-only rule, entry-valid line, TradingView DAILY line; v1.4 REAL 5d volume + SmallMidLive board + target floor ≥5% w/ honest R:R) |
| `fetch_rs.py` | v2.2 (2026-07-26 sheet/formula audit: NIFTY50 row's own 20_DMA now fed too — this script always deliberately skipped the NIFTY50 row from RS targets, but that also meant col E (20_DMA) for row 2 was never fed by anything and stayed on a live GOOGLEFINANCE formula forever, the one input `get_market_regime()` reads directly with NO yfinance fallback for the whole bot's bull/bear decision. Now computed from the ^NSEI daily series already downloaded as a benchmark, no extra API call; 50/200_DMA etc. deliberately left blank for row 2 as before — the regime gate never reads them; v2.1 2026-07-24: GOOGLEFINANCE migration slow lane — now also writes 20/50/200_DMA, 52_Weeks_Low/High, Avg_Volume_(20D), Pivot_Resistance, VCP Status, Days Since Low as Python-fed plain values, retiring their GOOGLEFINANCE formulas; lookback bumped 3mo→15mo to cover the 200-DMA/52wk windows from the same single download; same schedule (08:45+12:30 IST); v2.0 2026-07-17: + TRUE ATR(14) feed into Nifty200 col AC — old formula was ONE day's high−low feeding all SL/targets; + NIFTYBEES→^NSEI benchmark freshness fallback; workflow moved to own `fetch-rs` concurrency group after `trading-bot` group froze RS 07-13→07-17) |
| `fetch_live_prices.py` | v1.1 (2026-07-26 sheet/formula audit: the NIFTY50 index row was being SKIPPED entirely — same "NSE:XXX→XXX.NS" logic would have produced the invalid ticker "NIFTY50.NS" — leaving row 2's CMP/%Change on its original live GOOGLEFINANCE formula forever, the exact single-point-of-failure this script exists to remove, just for the ONE row `get_market_regime()` reads directly with no yfinance fallback. Fixed: NIFTY50 row now fed like every other row, using ^NSEI instead of being skipped; v1.0 2026-07-24: NEW — GOOGLEFINANCE migration fast lane. Every 5 min, market hours, writes real-time CMP/%Change/Live_Volume (new helper col AL) to Nifty200 — the 3 columns that need true intraday freshness, unlike fetch_rs.py's 2x/day slow lane. Verified before cutover: CMP/%Change tracked live GOOGLEFINANCE within a mean 0.13%/p90 0.4%/max ~1% deviation across all 200 symbols. Volume_vs_Avg % (col O) formula repointed to read Live_Volume instead of calling GOOGLEFINANCE(A,"volume") directly — same fail-open doctrine as fetch_rs.py: a failed fetch (per-symbol or whole-batch) leaves the existing cell untouched, never blanked) |
| `fetch_fii_dii.py` | v1.1 (2026-07-28: fixed a real silent data-loss bug — confirmed via live BotMemory + run logs that 2 real trading days, 2026-07-21 and 2026-07-23, lost every MKT_FII_*/MKT_DII_* key despite the script logging success. Root cause: 8 separate unbatched _bm_set() calls per run, each re-reading the whole ~3,800-row sheet with no pacing — same class of bug already fixed in refresh_cashwatchlist.py v1.3/fetch_earnings.py v1.1. Fixed with the same atomic single-write pattern: new _bm_set_many() reads once, writes once) |
| `fetch_index_meta.py` | v1.0 (2026-07-17: weekly Saturday — refreshes Nifty200 col AJ Options N50/YES/"" + col AK N200 YES/EX from NSE official lists; Telegram notice on drift; fail-open, never adds rows) |
| `fetch_dividends.py` | v1.0 (2026-07-30 NEW — weekly Saturday 11:15 IST, own concurrency group. Owner asked which cash stocks "benefit more with dividend, bonus etc." — nothing in the day-to-day cash/swing/options engine carried any dividend/bonus data before this (only a separate trailing `dividendYield` used by `generate_longterm.py` for the unrelated 12-month `LongTermSignals` sheet). NSE's real forward-looking corporate-actions calendar needs a protected session-cookie API this project deliberately avoids (same fragility class as the 2026-05-27 holiday-list outage) — so this feed is honestly TRAILING history, not a forward calendar. Adds 4 new columns via per-symbol yfinance `Ticker.dividends`/`Ticker.splits` calls (~0.8s/symbol, ~200 symbols in ~3 min, no bulk endpoint exists for this data unlike price bars): `Div_Yield_%_TTM` (col AO, computed directly from the raw trailing-365-day dividend sum ÷ live CMP, not trusted from yfinance's `.info` summary field which showed inconsistent units in spot checks), `Last_Div_Date` (col AP), `Last_Bonus_Split_Date` (col AQ), `Last_Bonus_Split_Ratio` (col AR — Indian bonus issues and true stock splits are indistinguishable in this data source, labelled honestly as "or"). Verified live: VEDL 10.11% yield (highest, correct — known high-payout PSU), IOC 7.15%, BPCL 7.12%, COALINDIA 6.33%, ITC 5.08%, TCS 4.56% — all matches known real payers; 186/200 symbols have dividend history, 136/200 have a bonus/split on record, 0 fetch errors. **DELIBERATELY NOT WIRED INTO ANY GATE** — informational only, same doctrine as `fetch_ath.py`/Sector_Trend/ShadowLedger. Fail-open per symbol (never-paid = correctly blank, not an error), columns found/created by header name, sheet grid auto-expanded 40→44 cols to fit) |
| `fetch_ath.py` | v1.0 (2026-07-29 NEW — weekly Saturday 10:45 IST, own concurrency group. Owner noticed DIVISLAB and EXIDEIND both got a "breakout" Breakout_Stage tag the same day despite not being the same setup; traced it to Breakout_Stage only ever comparing CMP to `52_Weeks_High`, never the stock's TRUE all-time high — a bounce toward a depressed 52-week ceiling (EXIDEIND, still 24.8% below its real 2024-06-25 high) gets the identical label as a genuine new high (DIVISLAB, which WAS its all-time high that day). Adds 2 new columns via one batched yfinance `period="max"` download (full history back to IPO, ~5,800+ daily bars for stocks like DIVISLAB/EXIDEIND): `All_Time_High` (col AM) and `%_from_ATH` (col AN) — verified live same session (DIVISLAB -0.43%, EXIDEIND -29.72%, matching the manual yfinance check). **DELIBERATELY NOT WIRED INTO ANY GATE** — owner instruction: "add the ATH check, but don't fix gate yet." `Breakout_Stage`/`Trade_Type`/every appscript.gs GATE are untouched; informational only, same doctrine as Sector_Trend when it was first added (v15.30). Fail-open per symbol, columns found/created by header name, sheet grid auto-expanded 38→40 cols to fit) |
| `appscript.gs` | v15.33 (LIVE, clasp-deployed 2026-07-29: v15.33 — same RR-math bug fixed for Positional + Swing, after explaining the v15.32 finding in more depth. Positional: ATR_TGT_POSITIONAL(4.0)/ATR_SL_POSITIONAL(2.5) was a FIXED RR of 1.6, and the v15.18 +5% floor could NEVER rescue it (proved algebraically self-contradictory: needs ATR%<1.25% to matter but only activates at ATR%≥1.3%, so both can never be true) — confirmed with HCLTECH's real 07-29 numbers, only a ~0.278×ATR-wide DMA-anchor coincidence could ever pass it. Fixed: ATR_TGT_POSITIONAL 4.0→4.7 (RR=1.88, unconditional). Swing (non-leader): ATR_TGT_SWING(3.0)/ATR_SL_SWING(2.0, never DMA-anchored) was a FIXED RR of 1.5, only rescued by the floor inside a ~0.09-percentage-point ATR% sliver (1.30%-1.39%). Fixed: ATR_TGT_SWING 3.0→3.8 (RR=1.9, unconditional). ATR_TGT_SWING_LEADER 4.0→4.8 alongside it (not explicitly requested, raised to preserve the same +1.0×ATR edge the top-3-ranked leader bonus always had over base swing). Stop distances unchanged, only targets moved out — same shape as v15.32. Together with v15.32, all 5 trade-type branches (Base/Momentum/Swing/Positional/Intraday) now clear MIN_RR unconditionally. v15.32 — intraday RR gate was mathematically impossible to pass. Owner asked why today's top gainers (35-symbol screenshot set) weren't reaching WAITING/TRADED; traced with live ScanDiag+Nifty200+AlertLog data (AlertLog was completely empty). RS≥5 gate correctly blocked 25/31 in-universe gainers (working as designed). But the "⚡ Intraday" branch's SL=CMP-ATR×1.5/target=CMP+ATR×2.0 is a FIXED RR of exactly 1.333 for every stock, always — MIN_RR=1.8 could never be satisfied. Confirmed live: DIVISLAB (today's best RS-passing candidate, RS 13.96, Master_Score 44) rejected with the exact math (SL ₹7547.81, target ₹8100.08, RR 1.33). `git log -p` showed MIN_RR/ATR_SL_INTRADAY/ATR_TGT_INTRADAY were all introduced together in commit 07b988c (2026-05-22, v15.6) — broken from day one, not a drift bug, live ~9 weeks; only the separate isMomentumEntry() breakout path (RR=2.0) could ever produce an intraday-style entry via the standard scan. Fixed (owner chose from 4 presented options): ATR_TGT_INTRADAY 2.0→2.8 (RR=1.867≥1.8), stop distance unchanged. Same root math issue found milder in Positional (baseline RR 1.6, floor can't rescue it) and non-leader Swing (baseline RR 1.5, narrow 1.30-1.39% ATR% band only) branches — NOT fixed this pass, owner still deciding, see SESSION.md 2026-07-29. v15.31 — scan diagnostic made actually readable. The v15.28 diagnostic (below) was built to explain why bullish-day candidates weren't reaching AlertLog's WAITING queue but only ever wrote to Apps Script's execution log, which needs a one-time GCP project link never done on this project (`clasp logs` → "GCP project ID is not set") — so it was write-only and the 07-21 mystery was never resolved, just silently recurring (confirmed: 07-13/16/17/20 all BULLISH all day with 0 entries, unexplained). Added a `skipReasons` Map that records the EXACT gate that rejects each stock, set inline next to each existing `continue` (11 gates + bearish hard-block + standard bullish filter) using that gate's own already-computed variables — cannot drift out of sync with the real logic. New `_writeScanDiag()` persists the top-5-candidate finding to a new "ScanDiag" sheet tab, overwritten every ~5-min scan (not appended, zero sheet growth), readable with normal Sheets access. Verified live same-session: tab appeared immediately, correctly showing today's top 5 (TITAN/NYKAA/RADICO/BHARATFORG/BIOCON) all rejected for "BEARISH + RS X < 15" — matches manual trace exactly. Zero gate/threshold logic touched. v15.30 — Sector_Trend column (Nifty200 col V, TAILWIND/HEADWIND) was computed every scan but read nowhere; now surfaced as an extra line on the premium options Telegram alert ("market+sector+stock sab same direction" / "extra caution") — owner's own 3-way alignment rule made visible, deliberately INFO not a new hard gate so it doesn't shrink the candidate pool. v15.29 — afternoon-move blind spot: CASH_ENTRY_WINDOW 10:30→13:30 (the 4%+ gap/catalyst intraday path only ever scanned till 10:30 AM), MOM_WINDOW_END 11:30→14:30 (sector-momentum scan same blind spot), RESULT_DAY_SKIP_PCT 6.0→10.0 (any isolated single-stock move >6% was auto-skipped as suspected earnings/corp-action unless 3+ other sector stocks also moved — ordinary 6-10% breakout days were being thrown out); CORP_ACTION_SKIP_PCT(15) and all bearish-regime thresholds deliberately untouched. v15.28: candidate-starvation diagnostic — read-only `[DIAG]` log line at the end of the WAITING-candidate loop showing the day's top-5 Master_Score stocks and whether each reached the queue (QUEUED/NOT-QUEUED + score/pri/rs/sector/ttype); added after tracing a 07-21 bullish-day mystery (only TITAN ever queued despite several other clean-on-paper STRONG BUY names) as far as static evidence allows — touches zero gate logic, pure Logger.log, wrapped in try/catch. v15.27: full-system audit — one-time header-position guard on the Nifty200 ATR (col 28) + Options-tag (col 35) reads in `_runScanner` (used by both the main scan and momentum scan); on header mismatch both fail SAFE to 0/null instead of trusting a shifted column, logs `[COLUMN-DRIFT]`; v15.26 OWNER RULE — option expiry = CURRENT month (≤5d rollover → next month, NEVER month+2; kills MIN_DAYS_BASE_ENTRY:40 that put a Jul signal on the dead Sep chain); option alert = intraday/1-2 din hold in easy Hinglish + entry-valid + TradingView lines; night-scan "Kaise trade kare" legend; v15.25 strike grid unified; deploy via `.\deploy_appscript.ps1`) |

### Long-Term Signals
| File | Version |
|---|---|
| `generate_longterm.py` | v1.7 (2026-07-26 full-system audit: internal `VERSION` constant was stuck at "v1.6" while the docstring header already said v1.7 — banner-text stale-stamp only, no logic affected) |
| `refresh_cashwatchlist.py` | v1.3 |
| `fetch_holidays.py` | v1.4 (2026-07-28: preventative hardening — same per-key-write shape as the confirmed fetch_fii_dii.py v1.1 data-loss bug, no confirmed loss here but fixed with the same proven atomic-write pattern anyway, cheap to apply and this data gates trading-day math bot-wide) |

### Core Infrastructure
| File | Version |
|---|---|
| `ai_client.py` | v2.5 (2026-07-26 full-system audit: `__main__` test-banner print() was hardcoded "v2.4", one release stale — cosmetic only, `python ai_client.py` direct-invocation path only) |
| `human_touch.py` | v2.5 (2026-07-26: `get_video_tags()` returned the SAME tags in the SAME order every call for a given mode — confirmed live, every morning reel had byte-identical hashtags. Same "100% fixed template" risk class as the v2.4 Facebook fix below, which already proved this pattern can cost real reach (lost Meta Recommendations eligibility). Fixed: a small `CORE_TAGS` set (brand + top search terms) stays first, everything else is now shuffled from the full relevant pool each call — genuine day-to-day variation, no caller code changed; v2.4 2026-07-23: FB_CAPTION_OPENERS/FB_HASHTAG_SETS + fb_caption_opener()/fb_hashtags() — Facebook captions were a byte-fixed template every post, a real contributor to the Page losing Meta Recommendations eligibility; wired into upload_facebook.py + daily-articles.yml's article-share step; v2.3 2026-07-19: all hook libraries rewritten number-first for retention; no invented stats; {holiday}/{week} placeholders preserved) |
| `token_refresh.py` | v2.2 |
| `content_calendar.py` | v2.4 |
| `money_funnel.py` | v1.2 (2026-07-20: article_cta_html() now plugs a rotating calculator too — was the only funnel surface v1.1's tool_plug() rollout skipped, and articles are the site's highest-traffic surface; v1.1 TOOL_PAGES + tool_plug() daily calculator rotation in funnel_block/comment_text — video→website traffic loop; single source of truth for broker links in CONTENT — Zerodha/Dhan/Groww/CoinDCX) |
| `indian_holidays.py` | current (no version tag) |
| `health_watchdog.py` | v1.2 (2026-07-20: CHECK 7 — payment-page integrity; fetches the live membership/shop/starter-kit pages daily, alerts if the UPI ID or WhatsApp number ever differs from the pinned known-good value — detects a UPI-swap attack via repo compromise, since payments have no gateway yet; v1.1 GH_TOKEN PAT expiry early-warning) |

### Content Generators
| File | Version |
|---|---|
| `generate_education.py` | v1.3 (weekly dup-guard: skips if Week N already on channel) |
| `generate_reel.py` | v2.7 (2026-07-26: owner asked for a "younger" voice — Edge TTS has only ONE Hindi male voice (Madhur) so there's no alternate to switch to within the ₹0/month constraint; added `VOICE_PITCH="+15Hz"` (tune after listening, not a calibrated constant) as the ₹0-compliant lever. Also: `draw_stat_block()` gained a real PIL-polygon triangle arrow next to the UP/DOWN pill instead of text alone — a real graphic reads at thumbnail size far better, same proven finance-content CTR technique added to `generate_reel_morning.py`/`generate_shorts.py` in the same pass; bullish/bearish accent colors also pushed more saturated for a stronger pop. Rendered and visually verified test thumbnails before shipping; v2.6 2026-07-24: last ZENO artifact removed — TTS voice was still `hi-IN-SwaraNeural` (the "wise female" voice ZENO was originally cast with) even after v2.5 removed the character from the script/visuals; switched to `hi-IN-MadhurNeural` (male) to match Amit's own persona and Short 2's existing voice; also added the same Ken Burns 1.0→1.08 zoom motion fix already proven in `generate_shorts.py` v3.11 — the reel's frame was 100% static for the full 45-60s, fully fail-open; v2.5 2026-07-22: ZENO cartoon persona retired — owner ("Amit") said it doesn't fit serious traders; prompt now writes in Amit's own voice, first-person, no kid character; added `fetch_eod_snapshot()` (real NIFTY close, yfinance, fail-open) so the evening reel is grounded in an actual number instead of 100% generic wisdom; thumbnail/frame character-compositing replaced with a live NIFTY stat block; "ZENO Ki Baat" branding → "Amit Ki Baat" across titles/descriptions/hashtags/badge) |
| `generate_reel_morning.py` | v2.8 (2026-07-26: same `VOICE_PITCH="+15Hz"` add as `generate_reel.py` v2.7 — applied only to the Hindi/Madhur path, not the English/Jenny fallback. Also added the same triangle-arrow icon to the main sentiment badge (BULLISH/BEARISH only, NEUTRAL gets none) and all 3 ticker rows (Crude/Gold/Bitcoin), replacing text-only "up"/"down"; bullish/bearish colors pushed more saturated. Rendered and visually verified a test thumbnail (all 4 arrows correct direction/color) before shipping; v2.7 2026-07-24: same voice fix as `generate_reel.py` v2.6 — `VOICE_HI` was still `hi-IN-SwaraNeural`, switched to `hi-IN-MadhurNeural` so every Hindi-voiced format speaks as one consistent person; added the same Ken Burns zoom motion fix (wraps the existing per-line frame `VideoClip` in a slow 1.0→1.08 zoom), fully fail-open; v2.6 2026-07-22: same ZENO retirement — this file already pulled 100% real data (Nifty/Crude/Gold/BTC/news) but still pasted a cartoon over it on every frame, not just the thumbnail; character removed from `build_thumbnail()`/`create_frame()`/`create_morning_video()`, replaced with a live Crude/Gold/Bitcoin ticker + NIFTY level now shown on every frame; "MORNING BRIEF" badge → "AMIT'S MORNING BRIEF") |
| `generate_shorts.py` | v3.13 (2026-07-26: same `VOICE_PITCH="+15Hz"` add, applied only to `VOICE_SHORT2` (Madhur/Hindi), not `VOICE_SHORT3` (Neerja/English). Also added the same triangle-arrow icon to `build_short_thumbnail()`'s category badge — direction inferred from the accent color the caller already picked (BULL_GREEN/BEAR_RED), no arrow for GOLD/neutral. Rendered and visually verified a test thumbnail before shipping; v3.12 2026-07-19: hook prompts prefer a REAL data number as first word; v3.11 retention pack: 20-25s payoff-at-end scripts + Ken Burns motion + 2s CTA) |
| `generate_articles.py` | current (2026-07-26 affiliate-link audit: `AFFILIATE_LINKS['broker']['india']`/`['investing']['india']` had their OWN separate, hardcoded, no-referral-code Zerodha URL (`https://zerodha.com/open-account/`) used to prompt the AI to insert natural in-body broker mentions — while the bottom-of-article CTA box (`money_funnel.article_cta_html()`) always correctly used the referral-coded one (`?c=YS3694`). Confirmed live in every article since April 2026, including today's: every article's inline body text sent readers to Zerodha WITHOUT the referral code while the CTA box below it had the right one. Fixed: both now import and reuse `money_funnel.LINKS['zerodha']` directly (fail-open to the old bare URL if the import fails) so they structurally cannot drift apart again — same "single source of truth" rule money_funnel.py's own docstring already states. usa/uk/insurance/loans affiliate links are genuinely separate programs with no money_funnel equivalent, left untouched. 2026-07-26 full-system audit (earlier same day): the article's AI-disclosure footer was a separate hardcoded string, never routed through `human_touch.ai_disclosure()` — a real violation of the "human_touch.py on ALL content, never bypass" rule, on the site's highest-traffic surface. Now calls `human_touch.ai_disclosure("en")` with a fail-open fallback to the old hardcoded string if the import fails; the "Not investment advice" legal line stays appended outside the disclosure call. Money-funnel CTA wiring was already correct, only the disclosure text was affected. 2026-07-20: meta_description/schema_json now run through `clean_ai_title()` too — was only applied to the title, so a raw "SandP 500" LLM artifact could still ship in the meta tag/JSON-LD; also now calls `performance_stats.write_data_json()` alongside the existing article perf block; 2026-07-16 credibility pack: price carry-forward cache `_data/prices_cache.json` + honest Fear&Greed + filler-phrase ban + number-honesty rule 14 + internal-link dedup; 2026-07-13 phrase-cooldown + perf block) |
| `performance_stats.py` | v1.1 (2026-07-20: `write_data_json()` publishes the same honest ledger stats to `_data/performance.json` — Jekyll `site.data.performance` — so the membership page can show the real, live win-rate/ledger table, not just generated articles; fail-open carry-forward, same doctrine as prices_cache.json; v1.0 2026-07-13 — History ledger → article performance block) |
| `publish_swing_board.py` | v1.0 (2026-07-23: NEW — reads `AlertLog` live, writes public-safe columns only (Symbol/Trade Type/Strategy/Breakout Stage/Trade Status/Days in Trade — never SL/Target/Entry/Position Size) to `_data/swing_board.json` for the swing-dashboard page's free teaser table; same fail-open/Jekyll-`_data` doctrine as `performance_stats.py`; replaces the old `SwingPublic` Google Sheets "Publish to web" embed that broke 2026-07-23 with no API to fix; run by `swing_board.yml`'s `*/15` cron (now a backup only — see `swing_board_session.yml`, 2026-07-28) plus `swing_board_session.yml`'s dense internal loop) |
| `trending_keywords.py` | v1.2 (2026-07-28: `_OFFTOPIC` exclusion set added — a homonym collision, not the v1.1 prefix bug: "Nifty" is also a Hazbin Hotel character name, so seed "nifty" legitimately whole-word-matched junk like "nifty vs velvette"/"nifty hazbin hotel"/"nifty and baxter" (confirmed live in `_data/trending.json`). All ranked below the top-15 `generate_articles.py` reads via `get_trending("finance", 15)` — zero live-article impact so far, but the same "ranks lower today, could rank higher tomorrow" fragility that already caused the v1.1 "ipod" incident. Live cache file cleaned of the 6 junk terms same session; v1.1 2026-07-20: whole-word seed filter in `_collect()` — YouTube autocomplete for seed "ipo" was returning "ipod"-class junk via shared prefix, not word-match; this had already published 2 live articles with "ipod" in the title/slug, both removed) |
| _(HerooQuest kids pipeline removed 2026-07-19 — generate_kids_video.py, kids_content_calendar.py, upload_kids_youtube.py, kids-daily.yml all deleted; channel retired as it cannot earn — see SESSION.md)_ |

### Upload & Distribution
| File | Version |
|---|---|
| `upload_youtube.py` | v2.4 (2026-07-22: the fallback thumbnail/metadata builder — only fires if the reel script itself failed to produce one — still pasted the ZENO cartoon and "ZENO Ki Baat" branding; owner retired ZENO for a serious-trader identity and this dead-path fallback was the one spot that got missed. Now matches: live NIFTY stat block, "Amit Ki Baat". `generate_reel.py`'s `save_meta()` now also writes `nifty_level`/`nifty_pct` into meta.json so this fallback has real data) |
| `upload_facebook.py` | v2.10 (2026-07-26 full-system audit: the v2.9 "fixed to read live version" claim was itself another hardcoded string literal — the file never had an actual `VERSION` constant, so both runtime print() banners were one hand-edit away from going stale again. Added a real `VERSION` constant, both banners now read it — can't silently drift again. v2.9 2026-07-24: full-system-audit cleanup — CLI --help text no longer says "ZENO" for the evening-reel prefix; runtime print() version stamp was hardcoded "v2.6" and silently stale through v2.7/v2.8. v2.8 2026-07-23: build_caption() uses human_touch.fb_caption_opener()/fb_hashtags() instead of a fixed f-string per mode — see human_touch.py v2.4; v2.7 2026-07-19 — kids branch removed; ZENO/morning/education/market paths unchanged) |

---

## STRICT RULES — NO EXCEPTIONS

### Rule A — Read Files Directly
- Claude Code reads files with the Read tool — no GitHub URLs needed.
- **ALWAYS** read the actual file before touching it. Never use session memory or previous reads.
- **NEVER** assume what a file contains. Read fresh, then edit.

### Rule B — Only State What You Can See
- **NEVER** give line numbers unless you can show the exact text proving it.
- **NEVER** say "yes it is fixed" without showing the actual content.
- When the owner corrects you — accept it, re-read, do not defend wrong answers.

### Rule C — Always Provide Complete Files
- **NEVER** provide partial code, snippets, or diffs.
- **ALWAYS** provide the complete file from top to bottom — every line.
- Even a 1-line fix requires the full file to be provided.

### Rule D — Verify Before Claiming Fixed
- **NEVER** mark a bug as fixed without reading the file and showing the corrected lines.
- **NEVER** claim a function exists without showing the read code.

### Rule E — Ask, Don't Assume
- If something is unclear — ask one specific question.
- Never fill gaps with assumptions. Wrong assumptions waste the owner's time.

### Rule F — Update .internal-ops.md After Every Change
- Every file change must be documented in `.internal-ops.md` immediately.
- This is the master system doc — if it's not there, the next AI won't know about it.
- Also update CHANGELOG.md and SESSION.md at end of every session.

### Rule G — Never Break What Works
- Do NOT break any working workflow. A broken workflow = zero income that day.
- Do NOT add paid services without explicit approval.
- Do NOT connect Dhan API or execute real trades (Phase 4 only — not yet).
- **Always use `human_touch.py`** — never bypass it for content generation.

---

## 🔒 NEVER CHANGE — OWNER-VALIDATED DECISIONS
*(Each line exists because of real money, real data, or an explicit owner decision. Do NOT "improve" these. If a change seems needed, present evidence and get owner approval FIRST. Full history in `.internal-ops.md` + memory.)*

### Trading — hard rules
| Rule | Why (evidence) |
|---|---|
| `BROKER_MODE = "PAPER"` — never connect Dhan API / place real orders | Phase 4 only, owner must explicitly start it |
| **Do NOT add DHAN_* secrets (API key/secret/client ID/PIN/TOTP) to GitHub until Phase 4 is actually being built** | 2026-07-20 security review: 5 unused Dhan secrets had sat in GitHub Actions secrets since Jan 2026 with zero code referencing them — live real-brokerage login credentials exposed for 6+ months for no reason. Deleted. Add fresh ones (and rotate PIN/TOTP on Dhan's side first) only when Phase 4 work actually starts |
| **Buy-side only** — never build/propose shorting or PE buying | Owner DECLINED sell-side 2026-06-05; bearish regime → options SKIP (option_intelligence v1.1) |
| **RS ≥ 5 gate** (bot Filter 7 + appscript GATE 3) — never lower | Data-validated 2026-07-15: ALL 6 target winners pass, 2/5 losers correctly blocked |
| **RSI hot-leader exception exactly as calibrated** — bullish + RSI 65–75 + stock "up on day" (now `day_pct > -0.3%`, a noise floor added 2026-07-21 v15.29 — was `> 0` exactly) = allowed; >75 hard block; fail-CLOSED without day data | 13-trade study 2026-07-15: no loser ever entered overbought; NYKAA-class winners were being refused. Don't revert to hard-65, don't raise above 75. The 0.3% floor is itself now locked too — evidence-based (3.5-week log audit: RSI was 45% of all entry blocks, 62% of those sat in the 65-75 window with a bullish regime, killed by single-tick day_pct noise) — don't raise the floor further without new evidence, don't remove it |
| **VOL gate = 1.5× PACE** (time-fair since v15.21/v1.1) — the 1.5× bar stays | Bar is deliberate (institutional footprint); only the measurement was made time-fair. Reversal-risk/ranking uses RAW relvol on purpose |
| **AppScript volume gates = time-fair PACE with EOD-identical bars** (v15.24: GATE 7 2.2×, BASE 1.4×, MOM/CASH 3.0×) — never revert to raw intraday readings, never change the bars without data | Volume_vs_Avg % is diff-form (+50 = 1.5×, owner screenshot v15.13); raw partial-day readings vs full-day averages structurally starved every intraday gate (the same bug fixed Python-side 07-16). Blank volume cells stay BLOCKED (volHasData) |
| **`_ENTRY_` memory keys = swing entries ONLY** (v15.23) — cash writes only `_CASH_` | The 3/day swing budget counter counts `_ENTRY_`; cash writing it too meant cash trades ate swing slots (v15.9 BUG-E's filter never worked) |
| **Earnings gate reads BotMemory ROWS via bm_data** (option_intelligence v1.2) — never revert to scanning the `_RUNTIME_MEM` string | fetch_earnings.py writes EARNINGS_* as sheet rows; the string scan matched nothing → CE suggestions possible the day before results |
| **ONE shared strike grid** (<100→1, <250→5, <500→10, <1000→20, <2000→50, ≥2000→100) in option_intelligence.strike_step + bot CE fallback + appscript _getStrikeInterval — change all three together or none | 2026-07-17: three divergent tables meant night vs entry alerts could name strikes on different ladders; the coarser grid is used because a coarse multiple always EXISTS when NSE's finer real spacing divides it (never alert a non-tradeable strike) |
| **appscript.gs `CONFIG.CASH_ENTRY_WINDOW` and trading_bot.py `CASH_ENTRY_WINDOW_END` must stay the same clock time — change both together or neither** | 2026-07-26 full-system audit: appscript.gs extended its scan-side window 10:30→13:30 (v15.29/30) to catch afternoon-building gap movers, but trading_bot.py's promotion-side cutoff was left at 10:30 — appscript.gs queued afternoon cash candidates that trading_bot.py then silently refused to ever promote, a real candidate-starvation bug live for ~2 days before caught. Same failure class as the strike-grid rule above: one engine scans/writes, the other promotes/reads, and any window/threshold that gates both sides must be edited in both files in the same commit |
| **MIN_SL_ATR_MULT = 0.75** — DMA-anchored SLs must leave ≥0.75×ATR room | BHARATFORG 07-16: DMA snap gave a 1.07% stop on a 2-4 week trade + fake RR 9.6 |
| **Owner's risk rule:** tight SL, target ≥5% *reachability-aware* (ATR%≥1.3 only), trail, option loss hard-capped 20%, option exit stock-anchored | v15.18 design; do NOT blindly hard-floor targets at 5% for low-vol names. Since bot v15.25 the PAPER LEDGER enforces the option rules too (20% est-loss cap + expiry-week backstop + v15.26 2-trading-day exit) — never revert options to generic stock time-stop/hard-loss monitoring |
| **OPTIONS = INTRADAY / MAX 1-2 DAY HOLDS, CURRENT EXPIRY** (owner rule 2026-07-18) — expiry picker: current month, ≤5d rollover → next month, NEVER month+2; swing/positional moves are traded in CASH/MTF, never as multi-week option holds; small/mid caps CASH only | Owner's own trading structure: liquidity lives in the current expiry (the old 40-day minimum put a 17-Jul signal on the dead Sep chain — no volume, ~20% spread); theta eats long option holds while the stock consolidates. Never restore MIN_DAYS-style far-expiry pickers or multi-week option hold wording |
| **Alert language = easy Hinglish + self-contained instructions** (v15.26): every entry/option alert states HOW to trade it (CASH/MTF/intraday), "Entry valid jab tak price SL ke upar hai", and which TradingView chart to check (DAILY swing / 15m cash+option timing / WEEKLY positional) | Owner + family act on these directly; a signal without "how" caused the MOTILALOFS Jul 1050 CE deviation (wrong strike, wrong expiry, wrong hold) |
| Empty AlertLog on a red/quiet day is CORRECT behavior, not a bug | NIFTY_MIN_PCT_BULLISH −0.30 + honest gates; 07-16 verified: 0 entries was right |
| **Fail-open for data feeds, fail-closed only where calibrated** | free-forever_self_repair: a dead feed must never kill the bot; the RSI exception is deliberately fail-CLOSED |
| **Nifty200 "ATR (14)" col = Python-fed true ATR (fetch_rs v2.0)** — never restore a GOOGLEFINANCE formula there | Audit 2026-07-17: old formula returned ONE day's high−low (ABB +60% off, IEX −26%) and fed every SL/target + option signal in both engines |
| **Nifty200 CMP/%Change/Live_Volume (fetch_live_prices.py, 5-min) + 20/50/200_DMA/52_Weeks_Low/High/Avg_Volume_(20D)/Pivot_Resistance/VCP Status/Days Since Low (fetch_rs.py v2.1, 2x/day) = Python-fed** — never restore a GOOGLEFINANCE formula in any of these 12 columns | 2026-07-24 full-system audit: ~25 of 37 Nifty200 columns were still live GOOGLEFINANCE formulas, including CMP itself — the single most foundational cell (`IF(C3="","",...)` gates nearly every other formula), so a GOOGLEFINANCE outage on CMP alone would zero the WHOLE scoring engine, same failure mode as the 2026-06-02 RS outage. Fast lane (CMP/%Change/live-volume) needs true 5-min freshness — both trading_bot.py and appscript.gs poll every 5 min during market hours; slow lane (DMAs/52wk/volume-avg/pivot/VCP/days-since-low) only needs 2x/day like RS/ATR already had. Verified before cutover: CMP/%Change tracked live GOOGLEFINANCE within mean 0.13%/max ~1% across all 200 symbols; 52-week hi/lo and Pivot_Resistance matched GOOGLEFINANCE exactly in spot checks. Column O (Volume_vs_Avg %) keeps its formula but now reads the new Live_Volume helper column (AL) instead of calling GOOGLEFINANCE(A,"volume") inline — never re-point it back |
| **Nifty200 row 2 (the NIFTY50 index summary row) is INCLUDED in the above rule, not exempt** — its CMP/%Change (fetch_live_prices.py) and 20_DMA (fetch_rs.py) must stay Python-fed from ^NSEI too, never re-skipped back to GOOGLEFINANCE or left out of either script's target loop | 2026-07-26 sheet/formula audit: the 2026-07-24 migration above covered all 200 stock rows but both scripts explicitly skipped the one row containing "NIFTY" in the symbol — leaving row 2 on its original live GOOGLEFINANCE formula, unnoticed, for CMP/%Change/20_DMA. This was worse than a normal missed row: `get_market_regime()` (trading_bot.py) reads row 2's CMP/%Change/20DMA DIRECTLY with **no yfinance fallback**, deciding bull/bear for the entire bot every tick — so the single-point-of-failure the 07-24 migration was built to close was still open for the one row that matters most. Fixed: fetch_live_prices.py v1.1 and fetch_rs.py v2.2 now feed row 2 too, using ^NSEI (matching `get_nifty_pct_change()`'s own existing fallback and fetch_rs.py's BENCHMARKS list) instead of skipping it |
| **fetch_rs.yml keeps its OWN concurrency group** — never share `trading-bot` group | Sharing it froze RS 07-13→07-17 (queued runs kicked daily by the session loop); sheet writes are atomic, sharing was never needed |
| **Option signals gate on Nifty200 col AJ "Options"** (N50/YES/"" from NSE's official F&O file, fetch_index_meta weekly) — never gate on hardcoded stock lists | 2026-07-17: hardcoded F&O list still contained IRCTC (derivatives removed by SEBI purge), TATAMOTORS (demerged), ZOMATO (renamed) — impossible/dead option alerts |
| **Nifty200 col B = 18 NSE macro sectors; col AK N200 = YES/EX membership** — never revert to fine-grained custom labels | 62 of the old 100 labels held ONE stock → auto "rank 1 leader" bonuses, dead momentum-sector detection, biased "Strongest:" line |

### Money & platform
| Rule | Why |
|---|---|
| **₹0/month forever** — no paid services without explicit owner approval | 6-family-member livelihood; free tiers only |
| **`human_touch.py` on ALL content** — never bypass | Content authenticity pipeline |
| **AI use is DISCLOSED, never evaded** — never build AI-detection evasion | FB flag 2026-06-07 was informational; disclosure + quality is the compliant path |
| **`ai_client.py` for every AI call** — actual production chain is Groq → Gemini → Templates; Claude/OpenAI are coded but gated behind `ALLOW_PAID_AI`, never set (corrected 2026-07-28, this row had the same "implies Claude/OpenAI run live" framing already fixed elsewhere in this file/requirements.txt/.internal-ops.md — missed here until now) | Never call AI APIs directly |
| **`money_funnel.py`** = single source of broker/referral links in content | |
| **Any new India broker/investing affiliate link (in `generate_articles.py`'s `AFFILIATE_LINKS` or anywhere else) must reuse `money_funnel.LINKS['zerodha']`, never a separate hardcoded URL** | 2026-07-26 affiliate-link audit: `generate_articles.py` had its own separate, no-referral-code Zerodha default for in-body AI-inserted mentions, silently diverging from the correct referral-coded link used in the same article's CTA box — live in every article since April 2026, real lost referral income. Fixed by importing `money_funnel.LINKS` directly instead of duplicating the string |
| Repo stays PUBLIC (GitHub Actions unlimited); `.internal-ops.md`/`SESSION.md`/`CHANGELOG.md` stay PC-LOCAL (gitignored) | Owner decision 2026-05-30 |

### Operational invariants
| Rule | Why |
|---|---|
| **Owner trades MANUALLY with real money off the Telegram alerts** | Alert quality = family income. Test + verify BEFORE every deploy; subscriber-facing wording must be honest (no fake VIX, no false-certain regime verdicts, no stale version stamps) |
| **AppScript deploys via `.\deploy_appscript.ps1` (clasp)** — never manual paste; bump `CONFIG.VERSION` every release | It is the single source for all message version stamps |
| `.bat`/`.cmd`/`.ps1` files stay **CRLF** (`.gitattributes` enforces) | smartsync.bat was silently broken for months by LF endings |
| NSE holidays only from the **official NSE list** — never guess/hardcode from memory | 2026-05-27 full outage from a wrong hardcoded list |
| Never ask the owner for chart screenshots for calibration — use yfinance | Owner directive 2026-07-15; charts only when HE offers them |
| T2 cell (AlertLog col T row 2) = automation master switch | YES = enabled |
| **A same-day code fix pushed during market hours will NOT take effect until the next fresh session, for ANY of the 3 session-loop workflows** — `trading_bot_session.yml`, `fetch_live_prices_session.yml`, `swing_board_session.yml` (2026-07-28) each check out code once, then loop internally via shell `while`+`sleep` for hours; GitHub Actions never re-checks-out code mid-job. Verified 2026-07-28 for `trading_bot_session.yml`: the day's session had already checked out at 11:43 IST, a real TSL-spam fix landed at 12:43 IST, and every tick for the rest of that session (through 15:01 IST) kept running the pre-fix code. If a same-day fix genuinely must take effect today, manually cancel + re-dispatch the session workflow (or its `*/5`/`*/15` backup cron — each of ITS invocations is a fresh checkout) to force a new checkout; otherwise it silently waits for the next session | Discovered while verifying the v15.32 TSL fix — it looked "unfixed" in live logs for hours after the commit landed, which was a checkout-timing artifact, not a bad fix |
| **`fetch_live_prices.yml`/`swing_board.yml`'s `*/5`/`*/15` crons are throttled by GitHub to ~3-5 runs/day (verified via `gh run list`, 2026-07-23 had ZERO `fetch_live_prices` runs all day) — never rely on them alone for freshness; the real density comes from `fetch_live_prices_session.yml`/`swing_board_session.yml`'s internal loops** — same platform limitation already documented for `trading_bot.yml`/`trading_bot_session.yml`. Any NEW high-frequency (`*/5`, `*/15`) cron added to this repo should be assumed throttled the same way and paired with a session-loop workaround from day one, not discovered later via missing data | Live evidence 2026-07-28: `fetch_live_prices.yml` feeds CMP — the single most foundational Nifty200 cell with no fallback — and was going stale for 1.5-2.5hr stretches during market hours; `swing_board.yml` feeds a public page that claims "auto-refreshes every 5 min" while actually refreshing ~3x/day |
| **All 3 session-loop workflows share their concurrency group with their own backup cron, and BOTH sides must use `cancel-in-progress: false`** — if the short backup cron used `cancel-in-progress: true` while sharing a group with the long session loop, a stray backup-cron trigger would kill the session loop mid-tick instead of queueing harmlessly behind it. `fetch_live_prices.yml` was flipped `true`→`false` for exactly this reason 2026-07-28 when `fetch_live_prices_session.yml` was added; `swing_board.yml` had no concurrency block at all before the same date (meaning it and `swing_board_session.yml` could have run concurrently and raced on the `git push`) | Same failure mode as sharing `fetch-rs`'s group with `trading-bot` did in July — the fix pattern is now standard: new session-loop workflow + existing backup cron always share one group, both `cancel-in-progress: false` |
| `health_watchdog.py`'s `EXPECTED_UPI`/`EXPECTED_WA` are PINNED values, not read from the live site | If the owner ever legitimately changes his UPI ID or WhatsApp number, update these constants in the SAME commit — otherwise the payment-integrity check (v1.2) will send a false "possible UPI-swap" alarm every day |
| **ShadowLedger (trading_bot.py v15.28) is observation-only — NEVER wire it to real/paper trading** without the owner explicitly reviewing the scored data first | Same doctrine as `ACCUM_AUTOTRADE`: it exists to measure whether the 1.5x volume gate is well-calibrated, not to trade on its own. Only promote a finding to a real gate-threshold change after enough scored rows exist and the owner has seen the numbers |
| **`fetch_ath.py`'s `All_Time_High`/`%_from_ATH` columns are informational-only — NEVER reference them in `Breakout_Stage`, `Trade_Type`, `Master_Score`, or any appscript.gs GATE** without the owner explicitly approving it first | 2026-07-29 owner instruction: "add the ATH check, but don't fix gate yet." Same doctrine as ShadowLedger/Sector_Trend (v15.30) — the data exists to make a real gap visible (Breakout_Stage can't distinguish a genuine new-high breakout from a bounce toward a depressed 52-week ceiling below an older, higher high), not to silently change scoring. Only wire it into a gate after the owner has seen the data and explicitly asks |
| **`fetch_dividends.py`'s `Div_Yield_%_TTM`/`Last_Div_Date`/`Last_Bonus_Split_Date`/`Last_Bonus_Split_Ratio` columns are informational-only — NEVER use them to filter/select CASH candidates or reference them in `Master_Score`/`Trade_Type`/`Priority_Score`/any appscript.gs GATE** without the owner explicitly approving it first | 2026-07-30 owner asked which cash stocks "benefit more with dividend, bonus etc." — full-system audit confirmed no dividend/bonus data existed anywhere in the day-to-day trading engine. Same doctrine as `fetch_ath.py`/ShadowLedger/Sector_Trend: the data is TRAILING history (NSE's real forward corporate-actions calendar needs a protected session-cookie API this project deliberately avoids, same fragility class as the 2026-05-27 holiday-list outage), shown for visibility, not silently wired into scoring. Only promote to a gate/filter after the owner has seen the data and explicitly asks |

---

## END OF EVERY SESSION

Before ending, update SESSION.md with:
- What was done this session
- Current version of every file changed
- Any new pending tasks discovered
- Any new known issues

Also update, every time without exception:
- `.internal-ops.md` — dated entry: what changed, WHY, what was verified, what was deliberately NOT changed
- `CHANGELOG.md` — same change in subscriber/owner language
- The CURRENT FILE VERSIONS table in this file (CLAUDE.md)
- The 🔒 NEVER CHANGE section in this file — add any new owner-validated decision made this session
- Memory (`MEMORY.md` + topic file) — so the next session starts already knowing

Then run:
```
git add -A
git commit -m "Session update: [brief description]"
git push
```

---

## KEY SYSTEM FACTS

- **AI Fallback chain (actual, in production):** Groq → Gemini → Templates. Claude/OpenAI exist as a coded last-resort in `ai_client.py` (v2.5, ₹0/month hardening) but are gated behind `ALLOW_PAID_AI=true`, which is never set anywhere in this repo (no workflow env, no secret) — so they never actually run, by design, guaranteeing zero AI spend. Never call AI APIs directly — always use `ai_client.py`
- **Content mode:** auto-detected by `indian_holidays.py` — market / weekend / holiday
- **Sheet:** `Ai360tradingAlgo` (Google Sheets)
- **T2 cell = automation on/off** (AlertLog col T row 2 — YES = enabled)
- **Paper trading only** — `CONFIG.BROKER_MODE = "PAPER"` in AppScript
- **Phase 3 pending:** Facebook Group posting (needs `publish_to_groups` token scope); Instagram full auto (needs Facebook Group fixed first — but also note: `upload_instagram.py` does NOT currently exist in the repo. It was built, then deliberately deleted twice as orphaned/never-wired code (last: commit `8edeace`, 2026-06-03 — "not imported anywhere, not invoked by any workflow"). Several docs kept describing it as "already built" for ~2 months after that deletion — corrected 2026-07-28. If Instagram auto-posting is ever picked up, it needs to be written from scratch, not just "unblocked")
- **Phase 4 planned:** Dhan API live trading — do NOT implement until explicitly asked

---

*See `.internal-ops.md` for full system documentation.*
*See `SESSION.md` for current task list and known issues.*
