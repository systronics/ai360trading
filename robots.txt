# ==========================================
# robots.txt for ai360trading.in
# Updated: May 2026
# ==========================================

User-agent: *
Allow: /

# Block Jekyll build output (not deployed, but keep as a guard)
Disallow: /_site/

# Block Jekyll system files
Disallow: /_drafts/
Disallow: /_includes/
Disallow: /_layouts/

# Block private areas
Disallow: /admin/
Disallow: /private/
Disallow: /temp/

# Block tag and category pages (thin/duplicate content)
Disallow: /tag/
Disallow: /tags/
Disallow: /categories/
Disallow: /category/

# Block Jekyll pagination — FIX: single rule covers ALL pages (was hardcoded page2–page10)
# At 4 articles/day this already covers 30+ pages — one rule handles all forever
Disallow: /page

# Block low-value and dead pages
Disallow: /darvas-box.html
Disallow: /darvas-box/
Disallow: /intraday-trading/
Disallow: /SYSTEM/
Disallow: /SYSTEM.md
# 2026-08-11: /plot-for-sale/(.html) removed from this block on purpose.
# The page never existed in this repo (confirmed via git log --all --full-
# history, zero results) — it's a phantom URL Google indexed once and kept
# a bare listing for, BECAUSE it was blocked here: a Disallow rule stops
# Googlebot from ever seeing the real 404, so it can't naturally deindex
# the URL. Unblocking lets Googlebot crawl it, get GitHub Pages' genuine
# 404, and drop it from the index over the next few crawl cycles — the
# correct fix for Search Console's "indexed, though blocked by robots.txt"
# warning. Do not re-add this line to "fix" a future reappearance of that
# warning; re-blocking would recreate the exact same problem.

# Bot optimization — allow Baiduspider (China/global index)
User-agent: Baiduspider
Allow: /

# Slow down aggressive SEO crawlers — no value in fast indexing
User-agent: dotbot
Crawl-delay: 20
Disallow: /

User-agent: AhrefsBot
Crawl-delay: 20
Disallow: /

User-agent: MJ12bot
Disallow: /

# Sitemap
Sitemap: https://ai360trading.in/sitemap.xml
